import { Typography } from "@mui/material";

const Upload = () => {
    return (
        <Typography variant="h4" sx={{ fontWeight: 800 }}>
            Upload
        </Typography>
    );
}

export default Upload;