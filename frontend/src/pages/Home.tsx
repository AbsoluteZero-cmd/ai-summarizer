import { Typography } from "@mui/material";

const Home = () => {
    return (
        <Typography variant="h4" sx={{ fontWeight: 800 }}>
            Home
        </Typography>
    );
}

export default Home;